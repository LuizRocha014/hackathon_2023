<?xml version="1.0" encoding="UTF-8"?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms" 
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="Hackathon_2.Views.PagesAdicionadas.InicioAdocao"
             Title="Adoção">
    <ContentPage.Content>
        <Grid>
            <Grid VerticalOptions="Center" HorizontalOptions="Center">
                <Label Text="Adoção iniciada com sucesso" TextColor="{DynamicResource Primary}" FontSize="35" HorizontalTextAlignment="Center"/>
            </Grid>
            <Frame BackgroundColor="{DynamicResource Primary}" VerticalOptions="End" Margin="25,0,25,10" Padding="12" CornerRadius="10">
                <Label Text="Ok" HorizontalOptions="Center" FontSize="Medium" TextColor="White"/>
                <Frame.GestureRecognizers>
                    <TapGestureRecognizer Tapped="TapGestureRecognizer_Tapped"/> 
                </Frame.GestureRecognizers>
            </Frame>
        </Grid>
        
  </ContentPage.Content>
</ContentPage>